# ===== Model roles (configure these in Texa Settings when possible) =====
LLM_REASONING_PROVIDER=qwen
LLM_REASONING_MODEL=qwen3.7-plus
LLM_REASONING_CREDENTIAL_ID=qwen
LLM_REASONING_API_KEY=sk-your-dashscope-key
LLM_REASONING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

LLM_VISION_PROVIDER=qwen
LLM_VISION_MODEL=qwen3.7-plus
LLM_VISION_CREDENTIAL_ID=qwen
# Integrated mode reuses the same Qwen credential slot.
LLM_VISION_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# split: vision extracts evidence, reasoning model solves it
# native: the configured vision model also completes image-task reasoning
LLM_MULTIMODAL_MODE=split

# Supported provider ids: deepseek, moonshot, qwen, gemini, openai, ollama,
# openai_compatible. The last option accepts any custom model name and endpoint.
# Existing LLM_BACKEND / DEEPSEEK_* / MOONSHOT_* variables remain readable for
# backward compatibility, but new settings are saved with the role variables above.

# ===== Embedding model =====
EMBEDDING_MODEL_NAME=BAAI/bge-small-zh-v1.5

# ===== Local data paths =====
DATA_DIR=./data
VECTOR_DB_PATH=./data/vector_db
BOOKS_PATH=./data/books
CHAPTERS_PATH=./data/chapters
PROGRESS_PATH=./data/progress
IMAGES_PATH=./data/images

# Desktop/offline mode defaults to local-only embedding files.
# Set to 0 only when the machine can download embeddings into data/models.
EMBEDDING_LOCAL_FILES_ONLY=1

# ===== MinerU external API / CLI (optional) =====
# Rented-GPU example: create an SSH tunnel, then use http://127.0.0.1:9001
# MINERU_API_URL=http://127.0.0.1:9001
# Optional CLI fallback; command must write MinerU outputs under {output}
# MINERU_CLI_COMMAND=mineru -p "{input}" -o "{output}" -m auto
MINERU_OUTPUT_PATH=./mineru_output
MINERU_TASK_TIMEOUT_SECONDS=3600
MINERU_TASK_POLL_SECONDS=2

# ===== Mistake OCR image preprocessing =====
MISTAKE_OCR_MAX_SIDE=1600
MISTAKE_OCR_JPEG_QUALITY=86

# ===== API access boundary =====
# Electron generates and injects a per-launch token automatically. Plain
# loopback development requests remain available unless token enforcement is
# enabled. Set a strong random token before exposing the service to a LAN.
# KAOYAN_API_TOKEN=replace-with-a-long-random-token
# Set to 1 to require the token even on loopback.
KAOYAN_REQUIRE_API_TOKEN=0
# KAOYAN_ALLOW_PRIVATE_CLIENTS is reserved for loopback-bound Docker networking.
