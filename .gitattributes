# Versioned embedding runtime assets are hash-addressed release payloads.
# Preserve their exact bytes on every platform; line-ending conversion would
# invalidate the manifest and break startup/repair verification.
assets/embedding-runtime/** -text
